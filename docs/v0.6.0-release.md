# v0.6.0 release record

## Candidate and scope

2026-09-07、Phase 6 完了後の release 判断が未記録だったため、利用者の継続指示で準備を開始した。基準は main `476304bf76153ffade23458d2100c13ed30e3973`。Phase 6 の共通 broker kernel とその保守、Issue #120 の Codex handover broker 統一を含む。Phase 7 は未着手。

以下は公開前の候補準備時点の記録。候補は `prep/v0.6.0` で準備した。release tag / version は `v0.6.0` / `0.6.0`。公開済み stable は引き続き `v0.5.0` であり、候補を公開済みと案内しない。clean な exact tag だけが `0.6.0` を返す。タグ作成前と Git のない未指定環境は `0.6.0-dev` を維持する。runtime と version resolver のアルゴリズムは変更しない。

変更概要は [release notes](v0.6.0-release-notes.md)、実施手順は [計画](superpowers/plans/2026-09-07-v0.6.0-release.md)。

## Historical authenticated evidence

- Phase 6 stage 2: [S2-4 検証記録](broker-kernel-stage2-validation.md)。基準実装 `36f02a8`、2026-09-07、4 broker の実 host gate が PASS。対象外の一部 gate は stage 1 証拠を採用し、rollback は not run とした記録を保持する。
- Issue #120: [handover 検証記録](codex-handover-broker-validation.md)。実装 `636128c`、2026-09-07、Codex / Claude の認証済み非対話 CLI から通常 sandbox tool を使い、両 agent の 6 gate が PASS。対話 TUI と実会話 ID 取得は not run。
- `636128c` から準備基準までの `src/`、`container/`、`profiles/`、`Containerfile` の一致を準備時に確認した（`git diff --exit-code` 成功）。履歴の結果を新規候補の認証済み smoke と読み替えない。
- 以前の CI `34111733529` attempt 1 の egress TLS timeout / Family PID 取得失敗は原因未確定。後続成功を原因解消の証拠にしない。

## Candidate gates

version/docs contract、lint、Codex / container 全 unit、4 broker socket suite、forced-unknown、差分・参照先を検証する。一時 clone 内だけの annotated tag で stable CLI と全 unit を検証し、Git のない image 配置では embedded version と fallback を確認する。required CI は最終候補 head の Unit tests / Podman integration（実 Podman 17 件、skip なし）を必要とする。

候補準備セッションではローカル実 Podman と新規の認証済み host smoke は not run（この環境に Podman と認証済み host runtime がない）。既存 project の更新・handover 移行も not run。新規 CI の Podman gate と履歴の認証済み smoke は別の証拠として提示する。

## Publication decision

2026-09-08（JST）に利用者が公開を承認した。承認候補は `62b104b25e081abc1d9ac7595fc238e0ff87db1e`。required CI [run 34122981699](https://github.com/jj1xgo/agent-container/actions/runs/34122981699) の初回実行で Unit tests / Podman integration がともにSUCCESSであることを公開直前に再照会した。

[PR #126](https://github.com/jj1xgo/agent-container/pull/126)をmain `b40544e5a52dca15da788e681829f9b99a5468e9`へマージし、承認候補とのtree一致を `git diff --exit-code` で確認した。immutable annotated `v0.6.0` はマージcommitではなく承認候補そのものを指す。remote tag objectは `2877a91365c16aa708ab04462817a6db8fbc5b7a`、peeled SHAは承認候補と一致する。

[GitHub Release](https://github.com/jj1xgo/agent-container/releases/tag/v0.6.0)を公開し、`publishedAt=2026-09-07T23:42:09Z`、draft=false、prerelease=falseを再照会した。CHANGELOGの公開日はUTCの2026-09-07を使用する。公開本文は状態表現と相対リンクを整え、既知のCI問題と未実施検証を保持した。

公開後の別変更でREADME stable=`v0.6.0`、main開発系列=`0.7.0-dev`、base=`v0.6.0`、fallback=`0.7.0-dev.0`、CHANGELOG、roadmapを更新する。runtimeとversion resolverのアルゴリズムは不変。今回の公開作業では新規認証済みhost smokeと既存project移行はnot run（公開とmetadata更新の範囲であり、移行は未承認）。前セッションの環境制約の記述は当時の記録であり、このhostの能力を示すものではない。

## Preparation checks (2026-09-07)

基準 `476304b` に候補差分を加えた状態で、lint、version/docs 80 件、Codex 49 件、container 1178 件、4 broker socket + forced-unknown 23 件が PASS（skip / ResourceWarning なし）。差分と新規文書を含む参照先検査も PASS。旧 metadata では新しい exact-tag 期待値等が4件失敗し、RELEASE_TAG / RELEASE_VERSION の更新で成功した。

候補 `f4540ac78768f39ec9c865253bbe0483ef1b7cb2` の一時 clone にだけ annotated `v0.6.0` を付け、CLI `agentctl 0.6.0`、Codex 49 件、container 1178 件が PASS。Git のない配置で embedded=`0.6.0` は `agentctl 0.6.0`、未指定は `agentctl 0.6.0-dev.0` と一致した。正式 repository / remote には tag を作成していない。一時 probe は `/tmp/v060-release-probe-4y0d56v0`（恒久的な配布物ではない）。

同候補の `476304b..f4540ac` を独立 reviewer が確認し、blocking / important の指摘なし。履歴実装との一致、版表示の候補 / stable の区別、移行と権限変更、未実施 gate を確認した。review は実環境の検証を代替しない。

この検証結果の追記は文書のみ。最終 head と required CI の結果は [PR #126](https://github.com/jj1xgo/agent-container/pull/126) に記録する。公開判断はその最終結果を照合してから行う。

## Candidate CI failure investigation

`0119a8a` の [CI run 34121789044 attempt 1](https://github.com/jj1xgo/agent-container/actions/runs/34121789044/attempts/1) は Unit tests PASS、Podman integration は17件中4 errors。egress のローカル TLS fixture を含む `podman run` が30秒で timeout。Family の codex / no-egress では PID 取得が30秒で失敗した。故障注入の codex / egress と claude / no-egress では意図した registration 拒否後、`podman stop` の失敗または timeout と、続く `podman kill` の非zeroにより cleanup failed を報告した。

該当 runtime、integration tests、workflow、Containerfile、profile は基準 `476304b` と同一で、production 差分は release metadata の2定数だけ。外部 TLS サービス障害、版情報の回帰、実際の container 残留とは断定できない。Podman の stop / kill stderr はこの経路では記録されず、すでに除去された場合との区別ができない。2件の後続 container 不在確認は未到達であり、cleanup は未確認として扱う。

初回ログを保持し、同一 commit の failed job を1回再実行した。[attempt 2](https://github.com/jj1xgo/agent-container/actions/runs/34121789044/attempts/2) は Podman integration PASS。Unit tests も成功状態で、今回の4 errorsは再現しなかった。再実行成功だけで原因解消とは扱わない。独立 reviewer もこの不確実性を確認し、release notes へ明記した。正式 tag / Release は未作成である。

失敗調査と再実行結果の追記後、文書テスト66件と差分検査は PASS。追記は文書のみであり、その最終 head の required CI は PR #126 で再確認する。

## Publication follow-up checks (2026-09-08 JST)

公開後のmetadata・文書更新で、lint、version/docs 80件、Codex 49件、container 1178件、4 broker socket + forced-unknown 23件、差分・変更文書の相対リンク検査がPASS。containerの成功実行とsocket gateは通常のhost権限で実行し、skip / ResourceWarningなし。初回のsandbox内container実行はsocket bind拒否等で失敗したほか、旧開発版を期待するCLI正規表現の更新漏れ1件を修正した。worktree作成時のmode 664によりmanaged policy検証も失敗したため、配布CLAUDE.md/statusline.shを仕様の644へ整えた。関連17件と全container suiteの再実行はPASS。runtime変更はない。

独立reviewで重要指摘なし。公開後の最終commitとrequired CIはfollow-up PRへ記録する。新規の認証済みhost smoke・既存project移行はnot run（上記公開作業の範囲）。実Podman gateは新規PRのCIで検証し、認証済みhost smokeとは区別する。
